{
  "name": "suanbird-particle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:static": "node scripts/verify-suanbird.mjs",
    "verify:goal": "node scripts/audit-goal-coverage.mjs",
    "verify:all": "npm run verify:static && npm run verify:goal && npm run build",
    "audit:screenshots": "node scripts/capture-audit-screenshots.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "three": "^0.184.0",
    "vite": "^6.4.2"
  },
  "devDependencies": {
    "fast-check": "^4.8.0",
    "vitest": "^4.1.7"
  }
}
