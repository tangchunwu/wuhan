import { describe, it, expect } from 'vitest';

// Placeholder smoke test so the suite runs before the real landmark tests land.
describe('test toolchain smoke test', () => {
  it('runs the vitest suite', () => {
    expect(true).toBe(true);
  });
});
