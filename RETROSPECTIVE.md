# Retrospective

## 2026-05-31 第一期独立项目与粒子星云底座

### Challenges

- 最初在 `D:\funapi` 内推进会影响现有项目，用户明确希望新开项目后，需要先恢复原项目并迁移到独立目录。
- `npm create vite` 在 Windows 路径参数下把目标目录生成成了 `D:\Dsuanbird-particle`，需要确认影响范围后整理为 `D:\suanbird-particle`。
- 默认 Vite 8 脚手架与当前 React 插件版本存在依赖冲突，需要改成稳定的 Vite 6 + React + Three.js 配置。
- 第一版 Bloom 与中心粒子亮度过强，移动视口下中心白光遮住了大量网络细节。

### Solutions

- 恢复 `D:\funapi` 的 `App.jsx`，卸载误加的 `three`，并执行 `npm run build` 确认原项目仍可构建。
- 将新建错位目录移动为 `D:\suanbird-particle`，所有后续实现都限制在新项目内。
- 用 React、Vite、Three.js、OrbitControls、EffectComposer 和 UnrealBloomPass 建立第一期全屏 3D 视觉底座。
- 实现球形粒子星云、外围节点网络、细线连接、中心能量爆发、长江/汉水光流、黄鹤楼/长江大桥线稿暗示和基础控制面板。
- 降低 Bloom 强度、中心粒子尺寸和核心材质透明度，让画面从过曝状态回到“中心爆发但外围网络可读”的状态。

### Learnings

- GraphPU 风格视觉的第一期不需要立刻进入 WebGPU，使用 Three.js 的 Points、LineSegments、ShaderMaterial 和后处理已经能快速建立方向感。
- 这种作品需要先把空间密度、黑底、细线、中心爆发和配色比例跑出来，再逐步加入原创蒜鸟形态，否则容易过早陷入复杂算法。
- 在 Windows 下使用 Vite 脚手架时，绝对路径参数可能被解析异常；后续可以优先在目标父目录内用相对目录名创建项目。

## 2026-05-31 第二期原创蒜鸟主体雏形

### Challenges

- 需要在不照搬现成 IP 形象的前提下，让粒子主体具备“蒜 + 鸟 + 蒜苗”的识别度。
- 第一阶段的中心能量核很亮，切换到主体形态时容易遮住蒜鸟轮廓。
- 粒子从星云切换到主体时，如果直接跳变会显得像普通模型出现，不符合“从城市星云中诞生”的目标。

### Solutions

- 为粒子系统增加蒜鸟目标点：蒜形身体、鸟头、尖喙、蒜苗叶片、双侧翅膀和尾部拖尾。
- 增加独立蒜鸟光线骨架，用发光线稿辅助识别主体轮廓。
- 增加 `星云态 / 蒜鸟态 / 城市态 / 爆发态` 模式切换，让作品开始具备持续演化结构。
- 使用跨帧 damp 过渡控制粒子 morph，粒子会从星云平滑聚合到蒜鸟目标坐标。
- 在蒜鸟态降低粒子点尺寸和隐藏中心爆发光束，让主体轮廓更可读。

### Learnings

- “主题识别”不能只靠粒子密度，需要轮廓骨架提供方向，否则中心高亮会把形状吞掉。
- 粒子 morph 的目标点分布比单纯模型线稿更重要，身体、叶片和翅膀需要占有不同粒子比例。
- 模式系统是后续城市态、网络态、爆发态继续扩展的关键接口。

## 2026-05-31 第三期武汉城市记忆层与网络态

### Challenges

- 城市元素不能做成旅游海报式堆叠，需要作为深空间中的结构暗示融入粒子网络。
- 五个模式同时存在后，控制面板需要继续保持克制，不能遮挡主画面或让按钮拥挤。
- 网络态需要与星云态区分开，但又不能把蒜鸟和武汉记忆层的视觉关系割裂。

### Solutions

- 强化长江大桥线稿，增加桥面、桥塔、垂线和拱形结构；强化黄鹤楼多层屋檐线稿。
- 新增城市记忆节点和樱花漂浮粒子层，让武汉元素从“图标”变成空间气氛和节点分布。
- 新增 `网络态` 模式，强化外围节点尺寸和细线透明度，突出 GraphPU 式城市关系图谱。
- 补齐网络态说明文案，并让五个模式按钮在控制面板中稳定排布。

### Learnings

- 武汉主题的识别度可以通过“长江/汉水流线 + 大桥结构 + 黄鹤楼轮廓 + 樱花漂浮”共同建立，不必依赖写实素材。
- 网络态最好作为独立观看模式，而不是只在背景里存在；这样用户能明确感到从城市星云到关系图谱的切换。
- 对这种全屏 3D 作品来说，UI 的职责是轻量调度状态，视觉叙事应该主要发生在画布内部。

## 2026-05-31 第四期动态图网络与交互回流

### Challenges

- 网络态如果只是调高透明度，会像静态背景层，缺少 GraphPU 式“图谱正在运算”的生命感。
- 每帧更新粒子、网络节点和全部边线会明显占用主线程，浏览器自动化验证中出现按钮响应超时。
- 控制面板开始加入更多目标功能后，必须继续保持克制，避免把作品变成参数面板。

### Solutions

- 为网络节点增加独立相位、呼吸缩放、切向漂移和鼠标扰动，边线跟随节点位置隔帧更新，形成轻量动态图网络。
- 增加画布点击爆发波：点击后粒子和网络节点向外扩散，并随时间衰减回流。
- 增加轻量、均衡、高密三档性能配置，用 `drawRange` 控制粒子和网络密度，并按档位调整 DPR 与 Bloom。
- 增加重置视角按钮，保留暂停/开启巡航、速度、呼吸强度和五种模式切换。
- 将默认均衡档降到更稳的负载，把边线更新改为三帧一次，优先保证交互响应。

### Learnings

- 第四期的关键不是堆更多点，而是让节点、边、鼠标和点击之间存在可感知的动态关系。
- 在 CPU 粒子阶段，`drawRange` 是很实用的性能阀门，可以先满足移动端和验证环境的响应，再把高密度留给后续 GPU 路线。
- 全屏 WebGL 作品的 UI 验证要留意多标签同时运行带来的干扰；保守默认档位比一开始追求密度更符合分期路线。

## 2026-05-31 第五期前置：蒜鸟主体精修与导出能力

### Challenges

- 原有蒜鸟主体已经能识别大轮廓，但蒜瓣、叶脉、羽线和尾迹层次还不够明确，容易在高亮粒子中被吞掉。
- 功能目标里要求粒子数量调节和截图导出，之前只有性能档位，没有连续密度控制。
- 截图导出需要从 WebGL canvas 取图，渲染器必须保留绘图缓冲，否则导出的图片可能是空白或不稳定。

### Solutions

- 调整蒜鸟粒子目标分布，增加蒜瓣纵向点位、五片蒜苗叶、尖喙延展和多股尾部拖尾。
- 强化蒜鸟线稿：新增蒜瓣内骨架、喙脊、眼圈、叶脉、翅膀羽线和三股尾迹。
- 新增 `粒子密度` 连续滑杆，与轻量、均衡、高密性能档位叠加控制实际绘制数量。
- 新增 `导出截图` 按钮，并为 WebGLRenderer 开启 `preserveDrawingBuffer`，支持导出当前画面 PNG。
- 浏览器验证了蒜鸟态、高密档、密度读数、截图按钮存在、画布渲染和控制台无错误。

### Learnings

- 蒜鸟的高级感来自“轮廓 + 内部骨架 + 粒子分布”共同作用，而不是把形象做得更卡通。
- 连续密度滑杆比单纯性能档位更适合调试视觉密度，可以在展览屏、官网首屏和移动端之间快速找平衡。
- 截图导出属于作品化功能，会带来轻微内存成本，后续第五期性能优化时需要把它纳入开关或演示模式策略。

## 2026-05-31 第五期：流光质感、演示模式与移动端降级

### Challenges

- 现有画面已经具备粒子、节点、城市线稿和蒜鸟主体，但缺少更连续的光轨层，远看时图谱运动感还可以更强。
- 控制面板功能逐渐增多，在小屏视口下容易超出可视高度，必须保证移动端仍能操作。
- 第五期目标要求能作为官网首屏、展览大屏或互动作品使用，因此需要一个无人值守的演示模式。

### Solutions

- 新增 `flow-trails` 流光轨迹层，用多条半透明曲线围绕蒜鸟、江河和外围网络缓慢旋转。
- 流光透明度会随网络态、城市态、蒜鸟态和爆发态变化，让状态切换更有连续能量感。
- 新增 `演示模式`，自动轮播星云态、网络态、城市态、蒜鸟态和爆发态，并强制保持自动巡航。
- 根据小屏或高 DPR 设备自动使用轻量档和较低密度，首屏默认降低负载。
- 控制面板增加最大高度和滚动保护，小屏下底部按钮仍可访问。

### Learnings

- 流光层可以用很低的几何成本明显提升“城市星云正在流动”的观感，比单纯增加粒子数量更划算。
- 展览/大屏语境需要自动叙事，演示模式能让作品不依赖用户操作也持续表达完整概念。
- 移动端降级不只是降低 DPR，也要降低默认绘制密度；否则控制台虽然无错，但交互会显得迟钝。

## 2026-05-31 第五期：粒子规模提升与档位校准

### Challenges

- 原始底层粒子池只有 4200，虽然适合早期验证，但离最终“桌面端 1 万到 5 万粒子”的目标还有距离。
- 直接提高粒子和边线数量会增加 CPU 侧位置更新成本，需要继续依赖档位和移动端降级兜底。
- 自动降级条件一开始把高 DPR 大屏也判定成轻量档，可能影响桌面展示密度。

### Solutions

- 将底层粒子池提升到 12000，网络节点提升到 2200，网络边线提升到 4800。
- 调整性能档位：轻量档保守，均衡档约 7440 粒子，高密档桌面可使用完整 12000 粒子池。
- 保留小屏默认轻量和密度 0.72；在当前 320px 验证视口下，高密档达到 8640 粒子和 1584 网络节点。
- 收紧自动降级逻辑：小屏直接降级，中屏高 DPR 降级，大屏桌面默认均衡。
- 验证扩容后星云态、蒜鸟态、网络态、高密档均可切换，控制台无错误。

### Learnings

- 第五期的规模提升需要和档位设计绑定，不能只改常量，否则移动端和浏览器验证环境会立刻吃紧。
- 12000 粒子已经进入“桌面展示级”的第一档，后续要继续冲 5 万以上，应该转向 Shader/Worker，而不是继续在 React 主线程上堆循环。
- 自动降级策略要同时考虑宽度和 DPR，大屏高 DPR 与手机高 DPR 不是同一种性能场景。

## 2026-05-31 第五期：视觉校准与截图成本优化

### Challenges

- 扩容后蒜鸟态中外围网络更密，主体线稿需要更清晰，否则蒜鸟轮廓容易被高密粒子和边线稀释。
- 小屏 HUD 文字占用较多上方空间，会削弱移动端第一眼的空间感。
- 浏览器验证工具在多次 WebGL 标签打开后出现 webview attach 超时，不能把工具背压误判为页面运行错误。
- 为截图导出长期启用 `preserveDrawingBuffer` 会增加 WebGL 常驻成本，不适合最终展示模式。

### Solutions

- 提高蒜鸟线稿在蒜鸟态的透明度，让蒜瓣、叶脉、眼圈、翅膀和尾迹更容易从粒子中读出来。
- 在蒜鸟态降低外围网络边线透明度和节点尺寸，让中心主体获得更高视觉优先级。
- 移动端收紧 HUD：降低 kicker 字号和字距，压缩标题与说明宽度，减少对主视觉遮挡。
- 移除渲染器常驻 `preserveDrawingBuffer`，改为点击导出后在下一帧 `composer.render()` 完成后取图。
- 使用 `npm run build` 和本地 HTTP `200` 验证代码与服务状态，记录浏览器截图验证受工具背压影响。

### Learnings

- 高密视觉不等于所有层都变亮；主题态需要主动压低背景网络，让蒜鸟主体成为清晰的视觉中心。
- 移动端第一屏空间比说明文字更重要，HUD 要服务作品，而不是抢作品的呼吸空间。
- 截图导出这类作品化功能要按需付费，避免为了偶发操作长期牺牲渲染性能。

## 2026-05-31 第五期：核心粒子 Shader morph 迁移

### Challenges

- 12000 核心粒子继续在主线程逐点更新 position，会挤压网络节点、边线、UI 控件和交互响应空间。
- Shader 迁移不能只搬动画公式，还要保持蒜鸟 morph、爆发态、鼠标扰动和点击回流的行为一致。
- 迁移中 `createNebulaPoints()` 的返回结构已经变化，主循环若继续读取旧的 `base / targets / phases` 会直接运行时报错。

### Solutions

- 将核心粒子的星云初始位置、蒜鸟目标位置和相位分别写入 `position`、`targetPosition`、`phase` attributes。
- 在顶点 Shader 中完成呼吸、旋转、爆发、星云到蒜鸟 morph、鼠标扰动和点击波扩散。
- 主动画循环删除核心粒子的 CPU position 写入，只保留 `uTime`、`uBirdMix`、`uBurstMix`、`uIntensity`、`uPointer`、`uClick` 等 uniforms 更新。
- 保留网络节点与边线的 CPU 动态更新，避免一次性迁移过多系统导致调试复杂度失控。
- 完成 `npm run build`、本地 HTTP 200、Chrome 只读页面验证、模式切换验证和截图非空检查。

### Learnings

- 核心粒子进入 Shader 顶点阶段后，主线程从“逐点写数组”变成“每帧更新少量状态”，这是后续冲 5 万粒子的关键前置。
- 粒子 morph 最适合拆成 attribute + uniform：attribute 存稳定几何目标，uniform 控制当前叙事状态。
- 分阶段迁移比一次性 GPU 化更稳；先把最大压力的核心粒子搬走，再考虑网络边线或更多后处理。

## 2026-05-31 第五期：48000 高密核心粒子池

### Challenges

- 目标要求桌面端达到 1 万到 5 万粒子，原 12000 粒子已经能展示方向，但高密冲击力还不够接近最终作品。
- 核心粒子上到 48000 后，如果档位比例不重校准，默认均衡模式可能过重，小屏和普通设备会被高密目标拖累。
- 网络节点和边线仍在 CPU 侧动态更新，不能和核心粒子一起盲目等比例扩容。

### Solutions

- 将核心粒子池提升到 48000，让高密档直接进入目标区间上沿。
- 将默认均衡档调整为约 18240 粒子，轻量档控制在约 6720 粒子，保持移动端和普通桌面可用。
- 小幅提升网络节点到 2600、边线到 6200，同时通过档位比例限制默认 CPU 网络更新压力。
- 调整粒子大小分层，让中心能量核、中层主粒子和远场微尘形成更明显的深度差。
- 完成构建、本地 HTTP 200、高密档 48000 读数、蒜鸟态/网络态切换和截图非空验证。

### Learnings

- Shader morph 迁移后，核心粒子数量可以显著提升，但 CPU 网络层仍然是下一阶段性能边界。
- 高密档要服务作品上限，默认均衡档要服务打开即顺滑，两者不应该用同一套密度假设。
- 粒子数量提升后，尺寸分层比单纯加亮更重要；否则密度会变成一团雾，削弱 GraphPU 式图谱结构。

## 2026-05-31 第五期：网络节点与边线 Shader 动态化

### Challenges

- 核心粒子已经迁移到 Shader，但外围网络节点和边线仍由 CPU 每帧写入 position，成为下一轮密度提升的主要边界。
- 边线需要跟随节点的呼吸、切向漂移、鼠标扰动和点击爆发；如果节点和边线公式不一致，会出现连接漂移或断裂感。
- 网络态必须保持 GraphPU 式“图谱正在运算”的感觉，不能因为迁移 Shader 而失去原有回弹和扰动。

### Solutions

- 为网络节点增加 `phase` attribute，并改用 `ShaderMaterial` 在顶点阶段计算呼吸、切向漂移、鼠标扰动和点击回流。
- 为每条边线的两个端点写入对应节点的 phase，让边线端点在 Shader 中使用同一套运动公式。
- 删除主动画循环中的网络节点数组写入、边线隔帧同步和 `needsUpdate` 标记，只保留 drawRange 与 uniforms 更新。
- 将网络时间、网络态强度、呼吸强度、节点大小、透明度、鼠标位置和点击波统一作为 uniforms 驱动。
- 完成构建、本地 HTTP 200、高密网络态、点击爆发、爆发态切换和截图非空验证。

### Learnings

- 对图网络视觉来说，节点和边线必须共享同一套运动函数，否则再高的密度也会显得“散”和“不可信”。
- Shader 化后，网络层从每帧写数千节点和上万端点，变成每帧更新少量 uniforms，为继续提升边线数量留下空间。
- 先 Shader 化核心粒子，再 Shader 化网络层，是从 demo 走向作品级图谱密度的更稳路径。

## 2026-05-31 第五期：武汉记忆热点浮现

### Challenges

- 城市层已有江河、大桥、黄鹤楼和随机城市节点，但外围网络仍偏抽象，用户不容易读出“武汉城市记忆生成图谱”。
- 直接叠加大量文字会破坏深空粒子装置感，让作品滑向旅游海报或说明页面。
- 热点节点需要跟随城市态和网络态显隐，同时不能遮挡中心蒜鸟主体。

### Solutions

- 新增十个武汉记忆热点：江汉路、汉口江滩、长江大桥、黄鹤楼、东湖、武大樱花、光谷、户部巷、琴台、汤逊湖。
- 每个热点由发光点、呼吸环、中心连线和低透明文字 Sprite 构成，作为 3D 空间中的记忆锚点。
- 热点在城市态完整浮现，在网络态以较低强度保留，让“城市内容”和“GraphPU 图谱”发生连接。
- 更新城市态和网络态说明文案，明确表达街区记忆锚点参与构成城市关系图谱。
- 完成构建、本地 HTTP 200、城市态文案、高密网络态和截图非空验证。

### Learnings

- 武汉元素不必全部写实建模；少量准确命名的空间锚点，可以把抽象粒子网络拉回具体城市记忆。
- 标签要像图谱里的低亮注释，而不是网页 UI；用 Sprite 放在 3D 空间中比 DOM 浮层更符合作品气质。
- 城市态负责“读得懂”，网络态负责“连起来”，两者共享热点节点能让主题叙事更完整。

## 2026-05-31 第五期：城市热点可探索交互

### Challenges

- 武汉记忆热点已经能在城市态和网络态浮现，但仍停留在“看得到”，还没有形成可探索的城市图谱体验。
- 热点的可见光点较小，如果直接用可见球体做拾取，鼠标命中会过于挑剔。
- 点击热点触发爆发时，需要从热点的 3D 世界坐标出发，而不是只按屏幕位置近似，否则会削弱“点亮城市节点”的感觉。

### Solutions

- 为每个武汉热点补充 story 文案，让 HUD 能显示“名称 / 气质 / 城市记忆”三层信息。
- 使用 `Raycaster` 检测热点，并为热点增加透明拾取球，扩大交互面积但不增加视觉噪声。
- 悬停热点时增强对应发光点、呼吸环和标签透明度，并在 HUD 中显示当前城市记忆。
- 点击热点时把爆发中心切换到该热点的 3D 世界坐标，让粒子和网络从被点亮的城市节点向外回流。
- 完成构建、本地 HTTP 200、城市态热点扫描命中、HUD 读数、点击保持和截图非空验证。

### Learnings

- 互动作品里的“可探索”不一定要复杂 UI；一个准确的 hover/click 反馈就能让图谱从背景变成对象。
- 视觉点和交互热区可以分离：可见元素保持克制，透明拾取范围负责易用性。
- 城市记忆节点被点亮后，点击爆发更像从武汉结构内部生长出来，而不是普通鼠标特效。

## 2026-05-31 第五期：100000 极限粒子档

### Challenges

- 作品目标里提到高性能机器挑战 10 万粒子，原高密档 48000 已接近桌面展示上限，但还没有真正的极限档。
- 直接把默认或高密档改成 100000 会破坏普通桌面和移动端体验，需要把“作品上限”和“默认顺滑”拆开。
- 网络层虽然已经 Shader 动态化，但 10 万核心粒子加更多节点仍然需要控制 DPR 和档位比例。

### Solutions

- 将底层核心粒子池提升到 100000，网络节点提升到 3600，边线提升到 9600。
- 新增 `极限` 性能档，单独使用 100000 粒子和 3600 网络节点，供高性能设备或展览机器手动开启。
- 重新校准轻量、均衡、高密档：轻量约 6500 粒子，均衡约 18000 粒子，高密约 48000 粒子，保留原有稳定展示层级。
- 控制面板性能档位改为四列，避免新增极限档后按钮拥挤。
- 完成构建、本地 HTTP 200、极限档 100000 读数、蒜鸟态、网络态和截图非空验证。

### Learnings

- “挑战 10 万粒子”应该是显式的作品上限档，而不是默认体验；让用户主动进入极限档比自动启用更安全。
- Shader 化后的核心粒子和网络层为 100000 档提供了基础，但 DPR 仍然是性能体验的重要阀门。
- 档位命名要表达风险和用途：轻量/均衡/高密用于日常展示，极限用于高性能机器和压力测试。

## 2026-05-31 第五期：自适应渲染质量守护

### Challenges

- 极限档已经能达到 100000 核心粒子，但不同机器、浏览器和屏幕 DPR 下性能差异会很大。
- 如果低帧率时直接降低粒子数量，会破坏极限档的“满粒子图谱”目标。
- 用户需要知道当前渲染质量状态，否则极限档看起来像一个没有反馈的压力开关。

### Solutions

- 在主循环中加入滑动 FPS 估计，根据帧率自动调整 `adaptiveQuality`。
- 性能压力较大时优先降低渲染 DPR，而不是减少粒子数量，保留 100000 粒子结构完整性。
- 帧率恢复后缓慢提升质量，避免 DPR 在阈值附近来回抖动。
- 控制面板新增 `FPS / 质量` 读数，让用户能看到当前质量守护状态。
- 完成构建、本地 HTTP 200、默认档读数、极限档 100000 读数、网络态和截图非空验证。

### Learnings

- 对大规模粒子作品来说，DPR 是比粒子数更柔和的性能阀门；优先保留结构，降低渲染成本。
- 自适应策略要慢降慢升，避免画面质量频繁闪动。
- 极限档不仅要“能打开”，还要有可解释的状态反馈，这样更接近真实展览环境的工程需求。

## 2026-05-31 第五期：沉浸展示模式

### Challenges

- 作品已经具备较完整的控制面板和 HUD，但在展览屏、官网首屏或录制画面中，界面会抢走粒子图谱本身的注意力。
- 隐藏 UI 后仍然需要可恢复，否则用户进入沉浸态后只能刷新页面。
- 沉浸模式应该只影响 DOM 界面，不应该改变 Three.js 场景状态、粒子密度或相机控制。

### Solutions

- 新增 `immersive` 状态和控制面板按钮，用于在普通调参态与沉浸展示态之间切换。
- 增加 `H` 快捷键切换沉浸态，`Escape` 退出沉浸态，保证隐藏按钮后仍能恢复界面。
- 使用 `.experience.is-immersive` 只控制 HUD 和控制面板的透明度、位移和鼠标事件，不触碰 WebGL 渲染逻辑。
- 完成构建、本地 HTTP 200、Chrome 自动化交互验证、沉浸态截图和非空图像检查。

### Learnings

- 这种作品需要明确分离“调参态”和“展示态”：前者服务创作与交互，后者服务观看与沉浸。
- 纯 CSS 的界面隐藏足够轻量，不会给 100000 粒子档增加额外渲染压力。
- 沉浸模式是作品走向大屏展示的重要一步，能让 GraphPU 式视觉密度成为第一主角。

## 2026-05-31 第五期：城市记忆层画面清洁与光核校准

### Challenges

- 城市热点文字 Sprite 在实际 WebGL 截图中出现黑色矩形贴片，破坏了深空粒子网络的高级感。
- 透明 Canvas 贴图、普通混合和像素级 alpha 清理仍不能稳定消除黑底，说明该路线在当前渲染链路里不够可靠。
- 城市态中心白光偏大，会吞掉长江大桥、黄鹤楼和二江结构，导致“城市记忆层”可读性下降。

### Solutions

- 移除常驻 3D 文字 Sprite，把城市记忆信息交给发光热点、呼吸环、连接线和 HUD hover/click 读数承载。
- 清理不再使用的 `createTextSprite` 与 `memory-hotspot-label` 分支，避免后续误以为场景里仍有标签贴片层。
- 为核心粒子 Shader 增加 `uParticleOpacity` 和 `uCoreBoost`，城市态降低中心粒子白雾，爆发态恢复冲击力。
- 城市态和网络态降低 Bloom 与中心爆发组透明度，让江河、桥体和城市节点从光核里露出来。
- 完成构建、本地 HTTP 200、城市态沉浸截图和非空图像检查。

### Learnings

- 3D 文字贴图在作品级画面里风险很高；如果透明链路不稳定，宁愿用空间锚点和 DOM 读数分工表达。
- 城市态不是越亮越好，它需要让武汉结构被看见；光核应该像能量心脏，而不是遮挡层。
- 作品推进到高密粒子阶段后，每次截图都可能暴露新的视觉层级问题，视觉 QA 和代码验证同样重要。

## 2026-05-31 第五期：蒜鸟主体识别度精修

### Challenges

- 高密粒子和中心白光会吞掉蒜鸟主体里的细节，导致身体、鸟头、眼睛和喙不够稳定可读。
- 原来的蒜鸟线稿在运行时被统一透明度覆盖，无法保留喙、眼睛、蒜苗、蒜瓣等识别锚点的强弱层级。
- 如果只继续增加线条，会让主体变杂；需要同时降低核心过曝，让已有结构真正露出来。

### Solutions

- 为 `makeLine` 生成的线条保存 `baseOpacity`，运行时按各自基础透明度呼吸，而不是所有线条同亮度。
- 强化蒜鸟线稿：提高蒜形身体、蒜瓣内骨架、鸟头、喙、眼圈、蒜苗叶脉、翅羽和尾迹透明度。
- 新增喙尖、蒜苗叶尖等发光锚点，补充蒜瓣根须线，让“蒜 + 鸟 + 蒜苗”的结构更完整。
- 在蒜鸟态降低 Bloom、核心粒子透明度和粒子中心高光，减少亮核吞细节的问题。
- 完成构建、本地 HTTP 200、蒜鸟态沉浸截图和非空图像检查。

### Learnings

- 蒜鸟主体的识别度不是靠更亮，而是靠轮廓、骨架、锚点和背景亮度之间的层级关系。
- 线稿系统需要保留每个部位自己的基础透明度，否则眼睛、喙和蒜苗这些关键特征会被平均掉。
- 高密粒子作品里，压光也是一种增强；把白光收住以后，主体生命感反而更明显。

## 2026-05-31 第五期：鸟头五官前景强化

### Challenges

- 蒜形身体、蒜苗和翅膀已经可读，但鸟头区域仍容易被中心粒子云和白绿光核半遮挡。
- WebGL 的 `LineBasicMaterial` 线宽基本固定为 1px，远景截图里眼圈和喙脊不够稳定。
- 单纯把五官加亮会与白绿粒子混在一起，需要用更明确的前景几何来增强识别。

### Solutions

- 将眼睛、眼圈、眉线、喙轮廓和喙脊推到更靠前的 z 层，作为前景五官骨架。
- 新增 `makeTube` 管状发光几何，用于眼圈和喙，让关键五官不再只依赖 1px 线条。
- 提高眼睛位置和粉色/白色高光强度，让鸟头和喙在蒜鸟态截图里更容易被捕捉。
- 为蒜鸟 glyph 关闭深度测试并提高渲染顺序，使主体线稿在高密粒子前保持可见。
- 完成构建、本地 HTTP 200、蒜鸟态沉浸截图和非空图像检查。

### Learnings

- 鸟头识别度受视角影响很大；前景几何能改善五官，但后续还需要蒜鸟态专属相机角度或主体微转角。
- 对远景视觉来说，线条语义和像素厚度同样重要；关键识别点可以用细管几何承担。
- 五官强化不能破坏整体抽象气质，保持“发光骨架”比做写实五官更符合数字艺术方向。

## 2026-05-31 第五期：蒜鸟态专属构图

### Challenges

- 五官和喙已经强化后，固定星云视角仍会让鸟头与中心亮核重叠，导致鸟的识别度不稳定。
- 直接大幅旋转主体会破坏 GraphPU 式中心星云构图，让翅膀和江河光带变成过强的横线。
- 蒜鸟态需要突出主体，但不能把武汉城市层完全拿掉，否则会削弱“从城市网络中诞生”的概念。

### Solutions

- 新增 `compositionTurn`，在蒜鸟态给根节点一个平滑的构图偏转，离开蒜鸟态后自然回到星云巡航。
- 蒜鸟态下给蒜鸟 glyph 轻微偏转、上移和前移，让鸟头、眼睛和喙从亮核中露到前景。
- 蒜鸟态下压低长江、汉水和城市线稿透明度，减少背景结构对主体的干扰。
- 保留外围网络和城市记忆节点，让主体仍然处在武汉城市星云结构之中。
- 完成构建、本地 HTTP 200、蒜鸟态沉浸截图和非空图像检查。

### Learnings

- 识别度不只是模型问题，也是构图问题；同一套几何换一个状态角度，主体可读性会明显变化。
- 蒜鸟态应该有自己的“舞台调度”，而不是复用星云态的默认观察角。
- 背景城市层在主体态要降为支撑，不应与鸟头和喙争抢第一视觉层级。

## 2026-05-31 第五期：模式切换叙事脉冲

### Challenges

- 五个模式已经可以切换，但视觉上还偏“按钮改变参数”，缺少从一个叙事状态进入另一个状态的仪式感。
- 模式切换需要触发中心能量反馈，但不能覆盖原有点击爆发逻辑，否则热点点击会失去空间来源。
- 过渡效果必须轻量，不能为了状态反馈再引入大量粒子或 CPU 更新。

### Solutions

- 新增 `mode-transition-pulse` 轻量几何层，由中心环、波纹线和辐射线组成。
- 在主动画循环中检测模式变化，触发中心扩散脉冲，并同步给核心粒子、网络节点和边线传入回流波强度。
- 将模式脉冲与点击爆发合成为 `totalWave`，让模式切换和手动点击共享同一套粒子/网络反馈，但各自保留触发来源。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化模式切换、三张 1280×720 截图和非空像素检查。

### Learnings

- 状态切换本身也可以成为作品叙事的一部分；中心脉冲让“星云态、城市态、蒜鸟态”之间更像同一个生命体在呼吸。
- 轻量线性几何足够表达能量波，不需要额外堆叠大量粒子。
- 验证 WebGL 作品时，构建、HTTP、DOM、截图和像素统计要组合使用，单一证据不足以证明视觉运行正常。

## 2026-05-31 第五期：真实暂停与恢复动画

### Challenges

- 原来的“暂停巡航”只停止相机自动旋转，并没有冻结粒子、网络、流光和模式动画，不完全符合功能目标里的暂停 / 恢复。
- WebGL 作品暂停时仍要允许用户拖拽观察，否则暂停会变成“锁死画面”，不适合检查蒜鸟主体和城市结构。
- 点击爆发、模式脉冲和 Shader 时间都依赖时间源，如果只把速度设为 0，容易造成局部动画或回流波不同步。

### Solutions

- 新增独立 `paused` 状态和控制按钮，文案在 `暂停动画` 与 `恢复动画` 之间切换。
- 将视觉系统统一改为 `visualElapsed` 时间：暂停时停止累加，恢复后继续推进，渲染帧和 FPS 估计仍保持运行。
- 相机自动巡航在暂停动画时关闭，但 OrbitControls 仍保留手动拖拽、缩放能力。
- 点击爆发改用视觉时间，流光轨迹的累加旋转也受暂停控制，避免暂停期间出现暗运动。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化暂停/恢复验证和暂停态截图非空检查。

### Learnings

- “暂停巡航”和“暂停动画”是两个不同功能：前者控制观看路径，后者控制作品生命节奏。
- 大型粒子场最好有统一的视觉时间源，这样 Shader、几何层、交互波和模式脉冲才不会各走各的时间。
- 暂停不等于停止渲染；持续渲染能保留交互观察、截图和状态读数，对作品验收更友好。

## 2026-05-31 第五期：武汉烟火气小火花层

### Challenges

- 完整目标里要求樱花粉、热干面金和橙红小火花作为辅助色，但现有环境层主要是樱花和城市节点，烟火气还偏概念化。
- 新增橙红元素容易让画面滑向廉价霓虹，需要控制数量、透明度和出现语境。
- 环境粒子层要服务武汉城市记忆，不能变成无意义的随机噪点。

### Solutions

- 新增 `ember-sparks` 环境粒子层，围绕户部巷、江汉路、光谷、汉口江滩等烟火/数字/江岸节点分布。
- 使用热干面金和橙红小火花色，并混入少量白色高光，保持与黑底、青绿主色的比例关系。
- 在城市态、网络态、爆发态提高火花透明度和尺寸，在蒜鸟态主动收弱，避免抢主体。
- 让火花层随视觉时间轻微旋转和漂移，补足“烟火气被编码进城市图谱”的空间表达。
- 完成 `npm run build`、本地 HTTP 200、城市态/爆发态 Chrome 截图和非空像素检查。

### Learnings

- 武汉气质不一定靠文字说明，少量精准的橙红/金色粒子就能把热干面、夜市和街巷火气带进画面。
- 辅助色最好依附在具体城市节点周围，而不是平均撒满全屏，这样既有叙事也不破坏高级感。
- 环境粒子层需要模式感知：城市态负责浮现，爆发态负责能量，主体态负责退后。

## 2026-05-31 第五期：悬停力场可视化

### Challenges

- 核心粒子和网络节点已经会响应鼠标扰动，但用户只能隐约感觉到变化，交互意图不够明确。
- 可视化 hover 反馈如果做得太亮，会像普通网页光标特效，破坏 GraphPU 式深空图谱质感。
- 暂停动画后仍需要能看到鼠标进入画面的反馈，否则检查静止构图时交互会显得断开。

### Solutions

- 新增 `pointer-field` 轻量力场层，由青绿色圆环和蓝绿色十字短线组成。
- 指针进入画布时力场淡入并跟随鼠标世界坐标移动，离开画布时淡出。
- 力场使用渲染帧时间做淡入淡出，让暂停动画时仍能响应鼠标进入和移动。
- 保持力场透明度较低，只作为“正在触碰粒子场”的提示，不抢蒜鸟主体和城市结构。
- 完成 `npm run build`、本地 HTTP 200、网络态鼠标移动截图和双截图差异检查。

### Learnings

- 交互反馈不一定要强，关键是让用户知道自己的动作已经进入 3D 空间。
- hover 力场应当服务粒子扰动，而不是取代粒子扰动；低透明几何比大面积光效更稳。
- 暂停态仍保留交互反馈，有助于用户检查构图、观察节点和理解空间深度。

## 2026-05-31 第五期：武汉记忆关系线

### Challenges

- 城市热点已经能被悬停和点击，但节点之间缺少明确关系，城市记忆仍偏“散点”，还没有完全成为图谱结构。
- 如果把所有热点互相连接，会造成画面杂乱，削弱长江、汉水和蒜鸟主体的视觉层级。
- 关系线需要在城市态和网络态强化，在蒜鸟态退后，避免与中心主体争抢。

### Solutions

- 新增 `WUHAN_MEMORY_LINKS`，用固定语义边连接江滩、大桥、黄鹤楼、户部巷、江汉路、东湖、武大樱花、光谷、琴台和汤逊湖。
- 使用二次贝塞尔曲线生成克制的分段 `LineSegments`，让关系线带一点空间弧度而不是平面连线。
- 关系线采用江水蓝绿和热干面金交替着色，城市态浮现，网络态更强，蒜鸟态收弱。
- 完成 `npm run build`、本地 HTTP 200、城市态/网络态 Chrome 截图和非空像素检查。

### Learnings

- 城市图谱的关键不是连接数量，而是连接语义；少量准确的边比全连接更有记忆结构。
- 关系线要像城市神经，不像装饰边框；透明度和模式显隐比线条数量更重要。
- 当武汉节点从“可读热点”进一步变成“互相连接的结构”，作品概念里的“蒜鸟从城市网络中诞生”会更成立。

## 2026-05-31 第五期：渲染能力与降级状态读数

### Challenges

- 项目已经有性能档位和自适应质量，但展览或官网首屏调试时，用户仍不容易判断当前设备处在桌面档还是移动降级。
- 完整目标里提到 WebGPU 探索，当前实现仍以 WebGL/Three.js 为主，需要把能力检测作为后续升级路线的可见入口。
- 性能状态提示不能变成技术面板，必须保持极简，避免喧宾夺主。

### Solutions

- 新增 `deviceStats` 状态，检测 `navigator.gpu`、当前 DPR 和是否触发移动/高 DPR 降级。
- 在控制面板中增加一行克制读数：`WebGPU 可用 / DPR x.x / 桌面档` 或 `移动降级`。
- 保留现有 FPS/质量读数，让运行质量、设备能力和粒子密度可以一起被快速判断。
- 使用桌面 1280×720 与移动 390×844、DPR 2 两组视口验证：桌面显示 18000 粒子，移动降级显示 4680 粒子。
- 完成 `npm run build`、本地 HTTP 200、双视口 Chrome 截图和非空像素检查。

### Learnings

- 大型 WebGL 作品不仅要自动降级，还要让调试者知道降级已经发生。
- WebGPU 探索可以先从能力检测和展示入口开始，不必过早重写渲染管线。
- 性能读数应当服务作品部署，而不是暴露过多工程细节；一行信息足够判断当前运行环境。

## 2026-05-31 第五期：局部点击扩散波

### Challenges

- 点击已经会影响 Shader 粒子和网络回流，但缺少明确的局部可视锚点，用户不容易看出爆发从哪里开始。
- 点击城市热点和点击空白空间都应该有反馈，但反馈位置必须跟随真实 3D 世界坐标。
- 视觉层不能太重，否则会压过中心爆发和模式切换脉冲。

### Solutions

- 新增 `click-ripple` 局部点击扩散层，由白金色波纹环和热干面金短射线组成。
- 点击热点时扩散波出现在热点世界坐标；点击空白时出现在鼠标映射的空间坐标。
- 扩散波与已有 `clickBurst` 共用衰减强度，和 Shader 粒子/网络回流保持同步。
- 采用轻量 `Line` / `LineSegments` 几何，不增加额外粒子模拟成本。
- 完成 `npm run build`、本地 HTTP 200、城市态两处点击截图和差异像素检查。

### Learnings

- 点击爆发需要“力学反馈”和“视觉锚点”一起成立；只有粒子移动时，用户不一定知道自己点亮了哪里。
- 复用已有点击状态比新增第二套时间状态更稳，可以避免扩散环和粒子回流节奏错位。
- 局部反馈要短促、克制，让它像城市节点被点亮，而不是普通按钮点击特效。

## 2026-05-31 第五期：作品化 HUD 叙事文案

### Challenges

- 原 HUD 模式说明保留了“第一期 / 第二期 / 视觉底座 / 主体雏形”等开发阶段语言，适合协作记录，但不适合作品最终呈现。
- 作品目标要求避免营销页和项目说明感，HUD 应该服务沉浸叙事，而不是解释开发进度。
- 文案需要同时说明五个模式的差异，并保持短句，避免遮挡主视觉。

### Solutions

- 将五个模式说明替换为作品化叙事文案，围绕城市星云、原创蒜鸟、武汉记忆、关系图谱和中心重生展开。
- 删除页面可见文案里的阶段词，让观众看到的是作品状态，而不是开发路线。
- 保留控制面板的模式标签和性能读数，确保作品可操作、可调试。
- 完成 `npm run build`、本地 HTTP 200、三种模式 Chrome 截图和页面文本检查，确认不再出现阶段开发词。

### Learnings

- 到第五期后，文案也是作品质感的一部分；开发语言会把观众从沉浸体验里拉出来。
- 模式说明应该描述“此刻画面在发生什么”，而不是描述“我们实现了什么功能”。
- 技术读数可以保留在控制面板，主 HUD 应该更像作品标题卡和状态诗句。

## 2026-05-31 第五期：目标验收审计索引

### Challenges

- 项目已经积累大量功能、截图和复盘记录，但缺少一个集中索引来回答“完整目标哪些已被证据覆盖”。
- 完整目标里既有工程项，也有审美判断项，不能把构建通过等同于最终作品完全达标。
- 后续 polish 需要稳定靶心，否则容易继续堆功能而不是针对真实缺口微调。

### Solutions

- 新增 `ACCEPTANCE.md`，把 `PROJECT_GOAL.md` 的目标拆成视觉底座、蒜鸟主体、武汉城市记忆层、交互、模式系统、性能与展示等验收组。
- 为每组列出当前实现证据和代表验证截图，明确哪些目标已有代码与运行时证据支撑。
- 单独列出尚需人工审美确认的项目，例如蒜鸟第一眼识别度、GraphPU 参考质感、城市结构可读性和默认展示模式。
- 完成 `npm run build`、本地 HTTP 200、页面状态检查和验收截图非空检查。

### Learnings

- 临近作品完成时，验收文档不是形式主义，而是防止目标漂移的锚点。
- 工程验证和审美确认需要分开记录；前者靠命令和截图，后者靠真实观看体验。
- 有了集中索引后，后续每次改动都能明确回答：它是在补完整目标，还是只是在增加噪声。

## 2026-05-31 第五期：城市与网络态曝光校准

### Challenges

- 城市态和网络态在高密粒子下容易被中心白光吞掉，长江大桥、黄鹤楼和图谱细线会变得不够可读。
- 网络态尤其容易从“高密关系图谱”滑成整块白色亮团，GraphPU 式结构感被削弱。
- 调光不能牺牲蒜鸟态和爆发态，否则主体识别度和中心重生的冲击力会一起下降。

### Solutions

- 针对城市态和网络态降低 Bloom、中心爆发可见度、核心粒子透明度和粒子中心高光。
- 为网络态单独增加曝光系数，压低节点透明度、线网透明度和节点尺寸，保留密度但减少白屏叠加。
- 使用三组截图对比城市态、网络态和蒜鸟态，并用像素均值确认网络态亮度从约 132 降到约 107。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化截图和非空像素检查。

### Learnings

- 高密粒子作品不能只靠统一亮度参数，必须按叙事模式分别控制曝光。
- 网络态的高级感来自“密而可读”，不是越亮越像图谱；线条层级比白光冲击更重要。
- 蒜鸟态、城市态和网络态需要不同的视觉优先级，调参时要同时看三张关键状态截图。

## 2026-05-31 第五期：默认自动叙事巡航

### Challenges

- 项目功能已经完整，但默认停在单一星云态时，更像可操作 demo，第一眼作品叙事不够主动。
- 演示模式原本需要用户手动开启，不利于官网首屏、展览屏或录屏场景。
- 自动切换不能干扰用户探索，手动点击模式后需要立即回到用户掌控。

### Solutions

- 将默认状态改为开启演示模式，页面打开后按星云态、网络态、城市态、蒜鸟态、爆发态自动推进。
- 抽出 `DEMO_SEQUENCE` 和 `DEMO_INTERVAL_MS`，让叙事节奏集中管理。
- 增加 `自动叙事巡航中 / 手动探索模式` 读数，让控制面板能说明当前观看状态。
- 手动点击任意模式时自动退出演示，避免用户刚选择状态又被定时器切走。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化阶段推进验证和三张截图非空检查。

### Learnings

- 作品级页面的默认状态应该主动表达概念，而不是等待用户发现所有模式。
- 自动叙事和手动探索需要清晰切换边界；用户一旦介入，系统应把控制权交还。
- 对全屏 3D 视觉作品来说，第一眼体验往往比新增一个功能按钮更接近最终目标。

## 2026-05-31 第五期：演示模式镜头编排

### Challenges

- 默认演示已经能自动切换模式，但镜头仍偏均匀自转，缺少展览作品里的“观看路径”。
- 不同状态需要不同构图：网络态要拉远看图谱，城市态要看江河结构，蒜鸟态要靠近主体。
- 自动镜头不能抢走用户拖拽后的控制权，否则互动体验会变得生硬。

### Solutions

- 为演示模式增加 mode-aware 镜头轨道，按星云、网络、城市、蒜鸟和爆发状态调整距离、高度和观察中心。
- 用户拖拽 OrbitControls 后，自动镜头暂停接管 8 秒，让手动观察保持优先。
- 演示模式下关闭普通 OrbitControls 自转，由更明确的相机轨道承担观看路径。
- 星云态和网络态镜头略拉远，减少中心白核占屏，让外围高密网络先成为第一视觉信号。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化模式/截图验证和非空像素检查。

### Learnings

- 作品化不只是模式切换，还包括镜头如何带观众看见每个状态的重点。
- 自动镜头需要有“礼貌”：系统可以引导，但用户一操作就应该暂时退后。
- 第一屏镜头距离会直接影响作品气质；稍微拉远能让 GraphPU 式深空间比白光冲击更先被看见。

## 2026-05-31 第五期：蒜鸟蒜瓣识别度增强

### Challenges

- 蒜鸟态已经能看出蒜苗、喙、眼点和翅膀，但蒜形身体中段容易被中心白核吞掉。
- 如果直接继续加亮主体，会破坏抽象数字艺术气质，让画面接近卡通图标。
- 新增结构必须增强“蒜 + 鸟 + 蒜苗”的第一眼识别，而不是变成复杂线稿噪声。

### Solutions

- 为蒜形身体外轮廓增加克制的管状光线，让主体边界在高密粒子里更稳。
- 增加前景蒜瓣筋线和底部根须光线，强化蒜瓣与根部识别锚点。
- 在蒜鸟态继续压低核心粒子透明度和中心白光高亮，让新增骨架从白核里露出来。
- 保持爆发态亮度独立，不因为主体态压光而削弱中心重生的冲击力。
- 完成 `npm run build`、本地 HTTP 200、蒜鸟态 Chrome 截图和非空像素检查。

### Learnings

- 蒜鸟识别度最关键的不是更亮，而是几个稳定语义锚点：蒜瓣、根须、蒜苗、眼点和喙。
- 抽象主体要靠“少量强结构 + 大量粒子气氛”成立，不能把每个细节都画实。
- 中心能量核要服务主体生命感，不能遮蔽主体本身；压光也是增强识别的一种方式。

## 2026-05-31 第五期：二江交汇脉冲层

### Challenges

- 长江和汉水已有流动光带，但二江交汇的城市结构信号还不够明确。
- 新增城市元素不能变成图标贴片，也不能抢掉黄鹤楼、大桥和中心蒜鸟主体。
- 城市态本身亮度已经偏高，新增脉冲层需要克制，避免进一步过曝。

### Solutions

- 新增 `river-confluence-pulse`，由低透明蓝绿/金色椭圆脉冲、沿江短线和江心光点组成。
- 将交汇层放在江河交汇附近，并随视觉时间轻微旋转与呼吸，强化二江交汇的空间感。
- 让交汇层在城市态明显、网络态低强度保留、蒜鸟态退后，符合不同模式的视觉优先级。
- 初版偏亮后收低环线、短线和核心光点透明度，使其成为辅助结构而不是额外白核。
- 完成 `npm run build`、本地 HTTP 200、城市态/网络态 Chrome 截图和非空像素检查。

### Learnings

- 武汉城市感可以通过“结构关系”而不是地标贴图表达，二江交汇就是很强的结构锚点。
- 城市元素新增时要先问它是否增强空间叙事，而不是只看它是否醒目。
- 高密粒子画面里，新增亮层要非常克制；一旦过亮，很容易吞掉原有城市线稿。

## 2026-05-31 第五期：城市态层级与曝光校准

### Challenges

- 城市态里中心白核和高密网络线过亮，长江、汉水、黄鹤楼和长江大桥容易被吞掉。
- 直接整体降亮会损失 GraphPU 式密集图谱质感，也会让城市态显得单薄。
- 城市元素需要从粒子网络里“浮现”，不能像额外贴上的图标层。

### Solutions

- 降低城市态 Bloom、中心爆发层、普通粒子核心和网络节点/边线曝光，让城市结构有可读空间。
- 提高长江、汉水、城市记忆线和城市节点在城市态的可见度，保持两江交汇的空间锚点。
- 为长江大桥和黄鹤楼线稿增加独立命名与亮度曲线，使它们不再和普通城市点一起被平均处理。
- 保持网络态和蒜鸟态的原有视觉优先级，避免城市态校准误伤 GraphPU 高密感和蒜鸟主体识别。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化三态截图和非空像素检查。

### Learnings

- 高密粒子作品的城市识别度不是靠继续加亮，而是靠分层曝光和语义锚点。
- 武汉元素最有效的表达顺序是：二江结构先成立，再让桥和黄鹤楼作为局部记忆点浮现。
- “亮”与“可读”不是一回事；把背景网络压出层级，反而能让数字艺术感更高级。

## 2026-05-31 第五期：城市记忆热点锁定交互

### Challenges

- 原有热点只能短暂悬停提示，点击后缺少持续反馈，还不像真正可探索的城市图谱。
- 热点本身很小，在高密粒子和自动镜头下，用户精确点中节点的成本偏高。
- 新增交互反馈不能破坏城市态已经校准好的层级，也不能把 HUD 做成喧宾夺主的信息面板。

### Solutions

- 增加 `lockedHotspot` 锁定态，点击武汉节点后保持城市记忆 HUD 和焦点视觉，点击空白处解除。
- 为城市记忆链接生成独立 `memory-focus-link` 层，锁定或悬停某个节点时只点亮相关路径。
- 新增 `memory-focus-beam`，用中心到记忆点的克制光束和锚点环表达“从城市网络读取记忆”。
- 将热点拾取从单纯射线命中升级为“射线命中 + 屏幕邻近兜底”，让小节点更容易被探索。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化点击锁定/空白解除验证、截图和非空像素检查。

### Learnings

- 互动作品里的城市节点不能只显示 tooltip；需要有持续的视觉状态，用户才会感觉自己真的进入了图谱。
- 小而克制的视觉对象必须配更宽容的交互命中逻辑，否则高级感会变成难用。
- “可探索”不是堆更多按钮，而是让画面里的结构本身可以被读取、锁定和响应。

## 2026-05-31 第五期：城市记忆回流轨迹

### Challenges

- 热点锁定已经能读出城市记忆，但还缺少“城市记忆回到中心蒜鸟”的动态叙事。
- 如果只加一条亮线，容易像普通指示线；如果加得太亮，又会破坏城市态刚调好的层级。
- 粒子回流需要影响主体系统，但不能让 10 万粒子出现大幅跳动或性能抖动。

### Solutions

- 新增 `memory-return-trails`，锁定热点后生成多条从记忆点回到中心的弯曲回流轨迹。
- 增加 `memory-return-packets` 光点包，让回流不只是线，而像数据/记忆信号正在流动。
- 为粒子 Shader 增加 `uMemoryFlow`，锁定热点时对附近粒子产生轻微向记忆点和中心的回流吸引。
- 将回流强度绑定城市态和网络态权重，让它服务“城市网络生成蒜鸟”的概念，而不是所有模式都抢戏。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化锁定/解除验证、截图和非空像素检查。

### Learnings

- 城市叙事不能只靠地标可见，还要让城市元素和中心主体之间发生运动关系。
- 回流效果最有效的是“少量清晰光点 + 克制曲线”，比铺满粒子更有高级感。
- Shader 里的局部吸引要很轻，作为空间呼吸的一部分存在，而不是把整个星云拉变形。

## 2026-05-31 第五期：蒜鸟城市记忆脉冲

### Challenges

- 城市态已经能锁定记忆节点并回流到中心，但切到蒜鸟态后，城市记忆和主体之间的关系还不够明确。
- 如果继续保留太多桥、楼、城市线稿，蒜鸟态会被城市结构抢走注意力。
- 记忆响应需要带上被锁定节点的颜色，但不能把蒜鸟做成廉价霓虹或卡通描边。

### Solutions

- 新增 `suanbird-memory-pulse`，由蒜瓣环、蒜苗线和翅膀光轨组成，专门承接锁定城市记忆。
- 锁定热点后切换到蒜鸟态，脉冲层会读取热点颜色，让黄鹤楼、长江大桥等记忆以对应色彩进入蒜鸟主体。
- 将城市线稿在蒜鸟态进一步退后，让城市记忆保留为主体脉冲，而不是继续以地标线稿抢前景。
- 脉冲强度绑定 `birdMix` 和 `suanbirdMix`，只有主体聚合时才明显出现，符合“蒜鸟吸收城市记忆”的叙事。
- 完成 `npm run build`、本地 HTTP 200、Chrome 自动化城市态锁定后切蒜鸟态截图和非空像素检查。

### Learnings

- 主题主体和城市内容之间需要跨模式因果关系；否则每个模式再漂亮，也会像独立特效集合。
- 蒜鸟态应该吸收城市记忆，而不是展示全部城市元素；退后地标线稿能让主体更高级。
- 颜色继承是很轻的叙事手段：黄鹤楼的金、樱花的粉、江水的青绿都能成为蒜鸟生命感的一部分。

## 2026-05-31 第五期：自动叙事记忆序列

### Challenges

- 作品已经具备手动探索能力，但第一次进入时还缺少能主动展示概念的叙事节奏。
- 自动演示如果只切换视觉模式，会像功能轮播；需要把武汉记忆点也纳入叙事推进。
- 自动叙事必须尊重用户控制，一旦用户手动切换模式，就不能继续抢占记忆锁定。

### Solutions

- 增加 `DEMO_MEMORY_SEQUENCE`，让自动演示依次读取黄鹤楼、长江大桥、东湖、江汉路、光谷等城市记忆。
- 新增 `memorySignal` 状态，把 React 控制层的叙事信号同步给 Three 场景层，由场景统一执行热点锁定。
- 抽出 `lockHotspot`，复用手动点击与自动演示的锁定逻辑，避免两套交互状态分叉。
- 增加“清除记忆锁定”按钮，允许用户显式退出记忆焦点。
- 完成 `npm run build`、本地 HTTP 200、自动叙事推进验证，以及手动模式退出自动演示的稳定性验证。

### Learnings

- 自动演示不是替用户操作，而是给作品建立第一眼的叙事入口。
- 叙事序列最好复用真实交互路径，这样演示状态和手动探索状态不会变成两套系统。
- 用户一旦介入，系统就应该把控制权交还给用户；互动艺术也需要清晰的控制边界。

## 2026-05-31 第五期：城市记忆叙事镜头

### Challenges

- 自动叙事已经能切换武汉记忆点，但镜头仍然主要围绕中心巡航，记忆点缺少被“看见”的空间权重。
- 如果镜头直接对准热点，容易丢掉中心蒜鸟和 GraphPU 式网络整体，画面会变成普通导览。
- 城市态、网络态、蒜鸟态对焦点的需求不同，需要同一套逻辑在不同模式下保持克制。

### Solutions

- 在自动演示镜头里加入 `lockedHotspot` 的世界坐标，让相机轨道和观察目标随当前记忆点轻微偏移。
- 按模式设置聚焦强度：城市态更靠近记忆点，网络态保留图谱结构，蒜鸟态只轻微继承城市记忆方向。
- 保留原有环绕巡航和用户操作保护，用户拖拽后仍会暂停自动镜头接管。
- 完成 `npm run build`、本地 HTTP 200、自动演示 HUD/记忆锁定验证，并保存 `verification-demo-memory-camera-focus.png` 截图。

### Learnings

- 叙事镜头不该只是相机移动，它需要和当前被读取的城市记忆建立空间关系。
- 对互动艺术来说，“看向某处”比“切到某处”更高级；缓慢偏移能保留生命感和整体空间。
- 高密网络态需要更克制的镜头权重，否则焦点会被线网淹没或把中心能量体挤出叙事。

## 2026-05-31 第五期：记忆焦点透镜

### Challenges

- 自动叙事镜头已经能偏向城市记忆点，但网络态线网密度很高，热点容易被绿色细线淹没。
- 继续单纯加亮会把画面推向廉价霓虹，也会破坏 GraphPU 式深空间层次。
- 新焦点层挂在旋转的 `root` 下，直接使用世界坐标会被父级二次变换，导致焦点偏离热点。

### Solutions

- 新增 `memory-focus-field`，由局部暗色透镜、呼吸光圈、短线扩散和环绕光点组成。
- 让焦点场继承当前记忆点颜色，长江大桥等节点会带出金色记忆锚点。
- 将热点世界坐标转换为 `root` 局部坐标，并让焦点场朝向相机，避免随根节点旋转后错位。
- 在网络态提高焦点场权重，同时使用暗色透镜制造局部留白，让热点更容易被看见而不是更刺眼。
- 完成 `npm run build`、本地 HTTP 200、自动演示锁定验证，并保存 `verification-memory-focus-field-lens.png` 截图。

### Learnings

- 高密粒子网络里的焦点可读性不能只靠亮度，局部降噪和留白同样重要。
- Three.js 里世界坐标和父级局部坐标的关系非常关键，视觉层跟随热点时必须明确坐标空间。
- 记忆节点需要像“被读取的空间事件”，而不只是一个 tooltip 或亮点。

## 2026-05-31 第五期：记忆网络扩散

### Challenges

- 记忆焦点已经能被看见，但相关城市节点之间还缺少“图网络正在传播”的动态关系。
- 主网络态线条非常密，新增传播层如果只用普通透明线，很容易被大面积青绿色网络淹没。
- 直接改主网络 Shader 会扩大风险，也会让抽象 GraphPU 网络和武汉记忆网络的语义混在一起。

### Solutions

- 新增 `memory-network-ripples`，复用武汉记忆点之间的连接关系，生成独立的激活边线。
- 锁定某个记忆点时，只点亮与该节点相连的关系边，其他记忆边保持极低透明度。
- 增加 `memory-network-ripple-packets`，让光点沿激活边传播，表达城市记忆在图谱中的扩散与回流。
- 将扩散层设置为前景渲染，并提高相关边的呼吸权重，保证它在网络态高密线网里仍可读。
- 完成 `npm run build`、本地 HTTP 200、自动演示锁定验证，并保存 `verification-memory-network-ripples-tuned.png` 截图。

### Learnings

- GraphPU 感不只是“很多线”，还要有局部激活和关系传播，用户才会相信这是活的图谱。
- 抽象网络和城市语义网络最好分层处理：主网络负责密度，记忆网络负责可读叙事。
- 在高密场景里，动态光点比静态亮线更容易让用户看出“传播方向”和“系统响应”。

## 2026-05-31 第五期：主网络记忆响应

### Challenges

- 记忆扩散层能表达城市关系传播，但主 GraphPU 网络本身还没有被记忆点真正牵引。
- 如果只继续叠加前景线条，作品会变成多个光效层，而不是一个统一的活系统。
- 主网络 Shader 同时驱动节点和边线，新增局部扰动必须避免破坏原有鼠标扰动、点击爆发和网络态呼吸。

### Solutions

- 为主网络节点和边线 Shader 增加 `uMemoryFocus`，传入当前锁定城市记忆点的世界坐标和响应强度。
- 在 `animateNetworkPosition` 中加入局部推拉波，让焦点附近的主网络节点和边产生轻微扩散、收束、回弹。
- 节点根据距离记忆点的远近提升透明度和点大小，边线根据距离轻微增强 alpha，让响应来自底层网络本身。
- 将响应强度绑定网络态、城市态和记忆扩散层，避免蒜鸟态被主网络抢走视觉中心。
- 完成 `npm run build`、本地 HTTP 200、自动演示锁定验证、运行时 Shader 错误检查，并保存 `verification-network-memory-shader-response.png` 截图。

### Learnings

- 一个视觉系统真正“活起来”，需要底层结构也响应叙事事件，而不只是上面浮一层效果。
- Shader 局部扰动适合做克制的生命感：幅度小，但和已有呼吸叠加后会让网络更像有弹性。
- 单帧截图很难证明动态回弹，后续可以增加短视频/GIF 或帧差检查来验证运动质量。

## 2026-05-31 第五期：控制面板收纳体验

### Challenges

- 控制面板功能已经完整，但在移动端会占据较多画面，影响全屏作品的第一视觉。
- 沉浸模式之前主要依赖键盘快捷键恢复界面，对触屏和展览场景不够友好。
- 新增收纳入口不能变成另一个显眼 UI，必须保持克制，不破坏主视觉。

### Solutions

- 增加 `panelOpen` 状态，桌面默认展开，小屏默认收起，让移动端优先展示主视觉。
- 新增 `panel-toggle` 极简按钮，用于展开/收起控制面板；沉浸模式下也能一键恢复界面。
- 调整桌面和移动端按钮位置：桌面展开时按钮收进面板范围，移动端展开时按钮位于面板上方，便于触控。
- 完成 `npm run build`、本地 HTTP 200、桌面/移动视口自动化验证，并保存 `verification-panel-toggle-desktop-final.png`、`verification-panel-toggle-mobile.png` 截图。

### Learnings

- 视觉作品的控制面板应该服务作品，而不是和作品竞争注意力。
- 移动端默认收起控制项，比压缩所有控件更符合“先看作品，再调参数”的使用顺序。
- 沉浸模式需要触屏可恢复路径，不能只依赖键盘快捷键。

## 2026-05-31 第五期：自适应密度守护

### Challenges

- 当前系统已经具备 10 万粒子和高密网络，但只靠 DPR 降级还不足以覆盖极限档或弱设备压力。
- 如果浏览器切到后台再回来，过大的帧间隔会让动画、相机和波纹出现明显跳变。
- 性能降级不能直接改用户选择的粒子密度，否则用户会感觉设置被系统偷偷改掉。

### Solutions

- 新增 `adaptiveDensity`，在 FPS 低于压力阈值时临时降低粒子、节点和边线的 `drawRange`。
- 保留用户设置的 `density`，最终绘制量由用户密度和自适应密度共同决定，恢复高 FPS 后再慢慢补回。
- 对 `frameDelta` 做 50ms 钳制，避免页面恢复、系统卡顿或切标签页后动画突然大跳。
- 在渲染状态中显示“密度守护”，让性能保护从隐藏机制变成可感知状态。
- 完成 `npm run build`、本地 HTTP 200、桌面/移动视口自动化验证，并保存 `verification-adaptive-density-desktop.png`、`verification-adaptive-density-mobile.png` 截图。

### Learnings

- 高密粒子作品的稳定性不能只靠降低分辨率，绘制数量也需要有自适应保护。
- 自适应系统最好不要覆盖用户显式设置，而是作为临时乘子叠加在用户选择之上。
- 帧间隔钳制是互动视觉作品的基础保险，能显著减少恢复页面时的突兀跳变。

## 2026-05-31 第五期：作品级取景预设

### Challenges

- 当前作品已经有多种视觉状态，但截图、录屏和阶段验收时仍需要手动调整相机。
- 自动叙事镜头会持续巡航，手动取景需要能短暂接管相机，同时不破坏 OrbitControls 的阻尼控制。
- 预设入口必须克制，不能让控制面板变得拥挤，也不能在移动端挤压主视觉。

### Solutions

- 新增 `CAMERA_PRESETS`，定义全景、蒜鸟、城市、网络四个常用取景位置和观察目标。
- 增加 `cameraPresetSignal`，沿用现有信号式状态通道，把 React 面板操作传给 Three 场景。
- 在动画循环中加入 `cameraPresetState`，使用缓动插值平滑移动相机和 `controls.target`。
- 点击取景预设会退出自动演示、切换到对应模式，并短暂暂停自动镜头接管，便于稳定查看。
- 在控制面板中新增 `view-row`，保持四个预设按钮紧凑排列，服务展览调试、截图导出和录屏构图。

### Learnings

- 对互动视觉作品来说，取景预设不是装饰功能，而是后续验收和叙事调度的基础设施。
- 相机预设应该接入真实 Three 相机，而不是只切换模式，否则无法保证可复现的展示画面。
- 自动镜头、手动控制和预设镜头需要明确优先级，才能避免用户点击后又立刻被巡航拉走。

## 2026-05-31 第五期：验收自检闭环

### Challenges

- 作品已经进入多层系统阶段，后续继续打磨视觉时容易无意破坏模式、取景、截图或性能档位入口。
- 内置浏览器验证偶发连接超时，不能把所有阶段验证都压在单一浏览器通道上。
- 验收文档需要同步记录新能力，否则项目状态会和实际实现逐渐脱节。

### Solutions

- 新增 `scripts/verify-suanbird.mjs`，对核心目标文档、五种模式、四个取景预设、截图导出、性能档位和控制面板收纳做静态自检。
- 在 `package.json` 增加 `npm run verify:static`，作为构建之外的轻量回归检查。
- 更新 `ACCEPTANCE.md`，把一键取景预设和静态自检纳入工程证据。
- 完成 `npm run verify:static`、`npm run build` 和本地 HTTP 200 验证。

### Learnings

- 对视觉项目来说，静态自检不能证明画面好看，但能守住关键入口和叙事结构不被后续迭代打断。
- 验收文档应该跟随功能同步更新，否则“已经做到什么”会越来越依赖记忆。
- 浏览器自动化和静态自检最好互补：前者看运行状态，后者守结构和回归入口。

## 2026-05-31 第五期：主网络沿边光脉冲

### Challenges

- 主网络已经有呼吸、扰动和记忆点牵引，但普通线网在静态截图里仍容易被看成装饰性背景。
- GraphPU 风格需要“关系正在传递”的感觉，不能只靠节点变亮或边线透明度变化。
- 当前粒子规模较高，新增动态图层必须控制绘制数量，避免影响已有自适应密度守护。

### Solutions

- 新增 `NETWORK_FLOW_PACKET_COUNT` 和 `network-flow-packets`，在主网络边线上生成轻量光点流。
- 光点复用主网络已有边坐标，不额外构建复杂图结构，沿边进行插值移动并加入极小抖动。
- 将光脉冲强度绑定网络态、城市态、爆发态和点击波，蒜鸟态则自动压低，避免抢中心主体。
- 光点数量受 `densityScale` 和自适应密度守护影响，弱设备或低密度设置下自动减少。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把主网络沿边流动纳入回归检查。
- 完成 `npm run verify:static`、`npm run build` 和本地 HTTP 200 验证。

### Learnings

- “图计算感”的关键不只是连线密度，还要让用户看到关系在边上移动。
- 复用已有几何数据可以用较低成本增加动态叙事，不必每次都上更重的模拟系统。
- 动态层要服从模式语义：网络态强化，蒜鸟态克制，这样作品中心不会被技术效果抢走。

## 2026-05-31 第二期/第五期：蒜鸟识别锚点增强

### Challenges

- 蒜鸟主体已经有粒子 morph 和线稿骨架，但在高密网络环境里，部分轮廓可能被整体光线吞掉。
- 目标要求“蒜 + 鸟 + 蒜苗”第一眼可识别，同时又不能变成实心卡通或儿童插画。
- 增强识别度需要服务中心主体，不能破坏当前克制的数字艺术装置气质。

### Solutions

- 新增 `suanbird-identity-anchors` 分组，专门承载蒜鸟识别锚点。
- 增加蒜瓣轮廓、顶部蒜冠、蒜苗冠结、鸟颈轮廓、脸颊呼吸线和翅尖光线。
- 锚点使用 `Line` / `TubeGeometry` 和加性混合，保持发光线稿语言，不引入实心模型。
- 在动画循环中给识别锚点增加轻微呼吸缩放和透明度增强，让它们只在蒜鸟态自然浮现。
- 更新 `ACCEPTANCE.md` 和 `verify:static`，把蒜鸟识别锚点纳入验收和回归检查。
- 完成 `npm run verify:static`、`npm run build` 和本地 HTTP 200 验证。

### Learnings

- 抽象角色的识别度不一定靠更多细节，而是靠几个高价值轮廓锚点。
- “像数字艺术”与“能读出主题”之间需要平衡：线稿锚点比实心模型更适合当前风格。
- 蒜鸟态应该把识别锚点作为短暂浮现的视觉提示，而不是永久高亮的卡通外描边。

## 2026-05-31 第三期：江河与地标动态脉冲

### Challenges

- 长江、汉水、黄鹤楼和长江大桥已经存在，但城市态仍偏静态，容易像一组线稿叠层。
- 武汉元素需要作为星云结构来源被感知，而不是只作为远处的地标符号。
- 动态增强必须避免旅游海报感和贴图感，继续使用粒子、线条和微光语言。

### Solutions

- 新增 `river-flow-packets`，沿长江和汉水曲线生成蓝绿流动粒子，强化“江河穿过星云”的可读性。
- 新增 `landmark-pulse-packets`，在长江大桥桥面/桥塔和黄鹤楼楼层线上生成克制的金色微光。
- 将江河流动强度绑定城市态，网络态轻微保留，蒜鸟态自动压低，保证主角不被城市层抢走。
- 使用少量 `PointsMaterial` 粒子，不引入贴图和写实模型，保持数字艺术装置风格。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把江河流动和地标脉冲纳入验收。
- 完成 `npm run verify:static`、`npm run build` 和本地 HTTP 200 验证。

### Learnings

- 城市元素的可读性不仅来自形状，也来自符合语义的运动：江河要流，桥和楼要像节点一样呼吸。
- 用少量运动粒子可以让静态线稿变成城市记忆结构，而不用走向写实建模。
- 模式语义仍然重要：城市态强化城市，网络态保留结构，蒜鸟态回到主体。

## 2026-05-31 核心概念：城市能量诞生涡旋

### Challenges

- 当前已有热点锁定后的记忆回流，但在未交互状态下，“蒜鸟从武汉城市网络中诞生”的常态叙事还不够直接。
- 如果只强化中心爆发，会变成单点光效；需要从外围城市网络向中心聚合，才能表达城市生成主体。
- 新增常态动效必须克制，不能让画面变成杂乱旋涡，也不能破坏蒜鸟态的主体可读性。

### Solutions

- 新增 `city-birth-vortex`，由外围向中心收束的螺旋线和粒子包组成。
- 光流在星云态轻微存在，城市态和网络态增强结构来源感，蒜鸟态继续向中心主体收拢，爆发态更强。
- 使用少量 `Line` 与 `PointsMaterial`，复用现有青绿、白光、热干面金、樱花粉色彩体系。
- 动画中让涡旋线缓慢旋转，粒子按流向从外层网络向中心移动，形成“城市记忆被编码后聚合成蒜鸟”的视觉语义。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把城市能量诞生涡旋纳入验收。
- 完成 `npm run verify:static`、`npm run build` 和本地 HTTP 200 验证。

### Learnings

- 核心概念需要常态可见的视觉语法，不能只依赖用户点击后的反馈。
- 从外围到中心的运动方向，比单纯中心爆发更能表达“诞生”和“汇聚”。
- 抽象视觉系统里的叙事层应该轻而持续，让用户不读文案也能感到城市网络正在生成主体。

## 2026-05-31 第五期：深空间星尘与景深暗场

### Challenges

- 主星云已经具备高密粒子和网络线，但远场空间主要依赖黑底和雾，深度层次还可以更丰富。
- 增加背景粒子容易破坏中心留白，或者把画面推向廉价满屏星空效果。
- 背景层需要受性能密度守护控制，不能在低端设备上无谓增加绘制压力。

### Solutions

- 新增 `deep-space-dust` 远场星尘层，使用 1800 个低透明度 `ShaderMaterial` 粒子构造深空间。
- 星尘分布避开中心，主要填充远场边界，增强深度而不抢中心白光和蒜鸟主体。
- 将星尘 drawRange 接入 `densityScale`，随用户密度和自适应密度守护一起降级。
- 星尘透明度根据模式调整：星云态更明显，蒜鸟态更克制，保持主体优先。
- 增加轻微反向视差旋转，让远场与主网络产生层次差。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把深空间星尘纳入验收。
- 完成 `npm run verify:static`、`npm run build` 和本地 HTTP 200 验证。

### Learnings

- 深空间感不靠把背景塞满，而靠远近层、中心留白和克制透明度。
- 远场粒子也需要纳入性能守护，否则越到作品后期越容易堆出隐藏成本。
- 轻微视差比强动画更适合背景层，它能增加深度，又不会和前景叙事抢注意力。

## 2026-05-31 第五期：中心关系光束

### Challenges

- GraphPU 参考图里很重要的一层是中心节点向外围网络辐射的细密关系线，现有中心爆发更像光核，还缺少“关系发散”的结构感。
- 新光束需要复用点击波、模式脉冲和网络态强度，变量计算位置如果处理不好，会和后续点击回流逻辑出现重复声明。
- 中心光束不能长期压过蒜鸟主体，否则蒜鸟态会从“城市网络中诞生”退化成普通中心光效。

### Solutions

- 新增 `CENTRAL_RELATION_RAY_COUNT` 和 `central-relation-rays`，用 `LineSegments` + `ShaderMaterial` 生成中心向外辐射的关系细线。
- 将点击爆发波和模式切换波提前统一计算，让中心关系光束、点击波纹和网络流光共用同一份 `totalWave` 状态。
- 光束透明度绑定网络态、城市态、爆发态和点击波，蒜鸟态自动压低，保留主体可读性。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把中心关系光束纳入验收闭环。
- 完成 `npm run verify:static` 和 `npm run build` 验证。

### Learnings

- GraphPU 感不仅来自外围高密网络，还来自中心与外围之间的方向性关系。
- 多个视觉层共享同一份事件波形，可以让点击、模式切换和关系辐射看起来像同一个系统在响应。
- 越靠近中心的强光层越需要模式约束，蒜鸟态要克制，网络态和爆发态才适合强化。

## 2026-05-31 第五期：江城声纹层

### Challenges

- 目标里有武汉方言幽默、街巷烟火气和城市人声，但现有表达主要依赖地标、江河和热点节点。
- 直接加文字标语会破坏数字艺术装置感，也容易让画面变成旅游宣传或营销页。
- 新层必须克制，不能抢走蒜鸟主体和 GraphPU 网络的主视觉。

### Solutions

- 新增 `JIANGCHENG_VOICE_PACKET_COUNT` 和 `jiangcheng-voice-waves`，用环形声波、街巷声纹线和微粒脉冲表达城市声音被编码进图谱。
- 声纹层跟随蒜鸟位置和姿态轻微漂移，让方言与烟火气围绕中心主体浮现，而不是贴在屏幕上的说明文字。
- 将声纹强度绑定蒜鸟态、城市态、爆发态、记忆锁定和点击波；网络态只保留低强度，保持整体克制。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把江城声纹层纳入验收闭环。
- 完成 `npm run verify:static` 和 `npm run build` 验证。

### Learnings

- 城市文化不一定都要变成地标，声音、方言和烟火气也可以转译成可运动的结构。
- 对这个作品来说，文字越少越高级；把语义做进粒子和线条，会比直接贴标签更贴合视觉系统。
- 武汉的松弛幽默感适合用轻微声波和小脉冲表达，不需要夸张卡通化。

## 2026-05-31 第五期：武汉三镇拓扑场

### Challenges

- 现有城市层已经有江河、地标、热点和声纹，但汉口、汉阳、武昌的城市空间关系还没有成为可见结构。
- 不能使用地图贴图或平面区块，否则会破坏深空间粒子网络的抽象质感。
- 三镇结构必须服务“武汉是网络来源”的概念，而不是变成新的旅游说明层。

### Solutions

- 新增 `WUHAN_TOWN_PACKET_COUNT` 和 `wuhan-town-topology`，用三个低透明空间场代表汉口、汉阳、武昌。
- 在三镇之间加入桥接曲线和流动微粒，让城市结构在城市态与网络态中形成可见的关系骨架。
- 将三镇拓扑透明度绑定城市态、网络态、爆发波，并在蒜鸟态自动压低，避免抢走中心主体。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把三镇拓扑层纳入验收闭环。
- 完成 `npm run verify:static` 和 `npm run build` 验证。

### Learnings

- 武汉的结构感不只来自长江和地标，三镇关系本身就是城市网络的核心骨架。
- 抽象城市拓扑比地图贴图更适合这个作品，它能保留数字艺术感，也能让主题更具体。
- 城市态负责显形，网络态负责连通，蒜鸟态负责收束，这种模式分工能避免画面过满。

## 2026-05-31 第五期：展演取景预设扩展

### Challenges

- 作品已经有三镇拓扑、江城声纹、中心诞生和江河交汇等新层，但原有四个取景预设只能覆盖基础状态。
- 展览、录屏和阶段验收需要更稳定的构图入口，不能每次靠手动旋转和缩放寻找画面。
- 某些构图不只是相机位置，还需要同步锁定对应城市记忆，否则叙事焦点不够明确。

### Solutions

- 将 `CAMERA_PRESETS` 从四个基础镜头扩展为八个展演镜头：全景、蒜鸟、城市、网络、三镇、声纹、诞生、江汇。
- 为三镇、声纹、诞生和江汇预设增加 `memory` 字段，点击时同步锁定长江大桥、江汉路、黄鹤楼或汉口江滩。
- 保持取景预设接入真实 Three 相机，继续使用平滑移动，不做 DOM 假切换。
- 微调取景按钮样式，让八个短标签在控制面板里保持紧凑，不喧宾夺主。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把八个展演预设和预设锁定记忆纳入验收闭环。

### Learnings

- 作品级交互不只是功能开关，也包括可复现的观看路径。
- 取景预设如果能同步叙事状态，就能从“镜头工具”变成“展演导演”。
- 越复杂的视觉系统，越需要稳定构图入口来服务验收、录屏和现场展示。

## 2026-05-31 第四期/第五期：主网络深度扫描 Shader

### Challenges

- 主网络已有节点呼吸、边线回弹和沿边光点，但线条透明度仍偏均匀，深空间层次还可以更强。
- GraphPU 参考质感需要“图谱正在被计算和读取”的感觉，单纯静态连线不够有运算感。
- 新增强必须留在 Shader 层，避免回到 CPU 每帧写大量节点或边线。

### Solutions

- 在主网络节点 Shader 和边线 Shader 中加入 `vDepthFade`，按视深衰减透明度和颜色强度。
- 增加 `vScan` 与 `spatialSweep`，让网络态中出现沿空间传播的克制扫描高光。
- 节点大小和边线透明度会随扫描波轻微增强，让图谱像被局部读取，而不是整屏同时发亮。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把主网络深度扫描 Shader 纳入验收闭环。
- 完成 `npm run verify:static` 和 `npm run build` 验证。

### Learnings

- GraphPU 感的关键不只是线多，还要有深度、扫描和局部读取的节奏。
- 透明度按视深衰减，可以让黑色空间重新拥有层次，避免高密网络糊成一片。
- 把质感增强留在 Shader 层，能继续保持高密粒子系统的性能边界。

## 2026-05-31 第二期/第五期：蒜鸟生命脉冲 Shader

### Challenges

- 蒜鸟主体已经能通过粒子 morph 和线稿锚点识别，但核心粒子在蒜鸟态的生命感还可以更强。
- 目标要求蒜鸟像从城市网络中诞生并持续呼吸，不能只是一个静态成形的粒子雕塑。
- 新增强需要保持抽象、高级和克制，不能把蒜鸟推向卡通外发光效果。

### Solutions

- 在核心粒子 Shader 中新增 `vLifePulse`，根据 `targetPosition` 计算蒜瓣、蒜苗和翅膀区域的生命波。
- 使用 `leafMask`、`bulbMask`、`wingMask` 区分蒜苗、蒜身和翅膀，让呼吸感沿主体结构分布。
- 在蒜鸟态中让目标点轻微纵向流动、横向舒展，并让粒子尺寸和青绿色光感随 `lifeWave` 变化。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把蒜鸟生命脉冲 Shader 纳入验收闭环。
- 完成 `npm run verify:static` 和 `npm run build` 验证。

### Learnings

- 主体生命感应该来自粒子自身的空间运动，而不是只靠外层线稿闪烁。
- 使用目标形态坐标来区分蒜苗、蒜瓣和翅膀，可以让同一个 Shader 脉冲更贴合角色结构。
- 克制的呼吸波比强闪烁更适合当前数字艺术装置方向。

## 2026-05-31 第五期：城市记忆编码雨

### Challenges

- 城市到中心的常态编码叙事还不够直接，不能只靠点击热点后的回流反馈。
- 新层不能依赖文字说明或贴图，否则会削弱数字艺术装置的抽象质感。
- 视觉上要表达“武汉记忆持续供能蒜鸟”，同时不能破坏蒜鸟主体可读性和整体性能边界。

### Solutions

- 新增 `CITY_MEMORY_CODE_COUNT` 和 `city-memory-code-rain`，把武汉热点转译成持续向中心投递的微粒编码流。
- 复用 `WUHAN_MEMORY_HOTSPOTS` 的语义坐标生成来源点，让城市记忆、热点交互和中心蒜鸟共享同一套空间逻辑。
- 在城市态和网络态中提高编码雨可见度，在蒜鸟态保留低强度供能，避免喧宾夺主。
- 将编码雨粒子数量接入 `densityScale`，让它跟随用户密度和自适应密度守护一起降级。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把城市记忆编码雨纳入验收闭环。

### Learnings

- 常态叙事比只靠点击反馈更重要，用户不交互时也应该能感到城市网络正在生成主体。
- 复用已有语义坐标可以降低复杂度，也能让不同视觉层之间更像同一个系统。
- 微粒编码比文字说明更适合这个作品方向，它让武汉记忆成为运动结构，而不是屏幕注释。

## 2026-05-31 第五期：武汉记忆谱系轨道

### Challenges

- 当前城市层已有热点、三镇、江河和编码雨，但热点关系在常态下仍偏分散，需要一个更整体的城市记忆外壳。
- 新增关系层必须保持克制，不能把画面推向旅游导览图，也不能遮挡中心蒜鸟生命体。
- 记忆谱系需要和已有热点、关系线、编码雨共用语义来源，避免形成另一套割裂的城市结构。

### Solutions

- 新增 `WUHAN_MEMORY_ORBIT_PACKET_COUNT` 和 `wuhan-memory-orbit-field`，用低透明轨道包裹中心外层空间。
- 复用 `WUHAN_MEMORY_HOTSPOTS` 与 `WUHAN_MEMORY_LINKS` 生成记忆弦线，让热点关系成为环绕中心的空间谱系。
- 新增 `wuhan-memory-orbit-packets`，让微粒沿轨道与热点之间缓慢游走，表达城市记忆持续被编码和读取。
- 将谱系轨道透明度绑定城市态、网络态、爆发态和热点锁定；蒜鸟态自动压低，保护主体识别度。
- 接入 `densityScale`，让轨道微粒跟随用户密度和自适应密度守护一起降级。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把武汉记忆谱系轨道纳入验收闭环。

### Learnings

- 当视觉层越来越多时，新增层应该承担清晰叙事职责，而不是只增加复杂度。
- 城市记忆需要既有“点”的可探索性，也有“层”的整体感；谱系轨道能把二者连接起来。
- 复用热点和关系线数据，比重新造一套轨道坐标更容易维持武汉城市骨架的一致性。

## 2026-05-31 第五期：自动演示诞生序章

### Challenges

- 当前演示模式已经能轮播模式和锁定记忆点，但打开页面后的叙事节奏还偏“功能轮播”，缺少作品开场的导演感。
- 目标强调“蒜鸟从武汉城市网络中诞生”，默认演示需要让用户不操作也能看懂星云、城市、图谱和蒜鸟之间的生成关系。
- 新增叙事不能变成大段说明或营销页文案，仍要保持控制面板克制，把主要表达留给 3D 画面。

### Solutions

- 新增 `DEMO_PROLOGUE_SEQUENCE`，把演示模式整理成星云、网络、城市、蒜鸟、爆发五段诞生序章。
- 每段序章同时指定视觉模式、武汉记忆焦点和一条克制的 `beat` 文案，让镜头、热点锁定和 HUD 状态同步推进。
- 新增 `publishDemoBeat()`，统一发布当前演示段落，避免模式、记忆点和文案分别维护导致叙事不同步。
- 将演示定时器依赖收敛到 `demoMode`，避免模式切换导致定时器反复重建，保持自动展演节奏稳定。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把自动演示诞生序章纳入验收闭环。

### Learnings

- 作品默认状态很重要；一个好开场能让复杂系统先被理解成叙事，而不是参数集合。
- 演示模式不只是自动切换按钮，它应该像展演导演一样安排视觉段落、城市记忆和中心主体关系。
- 克制短句比长说明更适合这个作品；文字只提示当前段落，真正的叙事仍由粒子、线条和镜头完成。

## 2026-05-31 第五期：首屏作品化展示

### Challenges

- 桌面端控制面板默认展开时，第一眼会更像调试工具，而不是官网首屏、展览大屏或互动视觉作品。
- 目标要求控制面板克制，不能喧宾夺主；但调试和手动探索能力仍要保留。
- 移动端已经默认收起面板，桌面端也需要统一“先看作品，再打开工具”的体验顺序。

### Solutions

- 将 `panelOpen` 初始值改为 `false`，让桌面和移动端默认都进入收起控制面板的作品观看状态。
- 保留右下角 `panel-toggle` 作为极简入口，用户需要调模式、密度、取景或截图时仍可一键展开。
- 为收起状态下的 `panel-toggle` 增加轻微透明度，让入口存在但不抢主视觉。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把首屏默认收起控制面板纳入验收闭环。

### Learnings

- 对全屏 3D 视觉作品来说，默认界面状态就是作品的一部分，不只是 UI 偏好。
- 控制面板越强大，越应该默认退后一步，把第一眼留给粒子网络和蒜鸟主体。
- 展示型项目的“可操作”与“先沉浸”并不冲突，关键是入口清楚但视觉权重足够低。

## 2026-05-31 第五期：WebGL 上下文稳定提示

### Challenges

- 当前作品已经进入高密粒子、Bloom 和多层 Shader 的展示阶段，浏览器或显卡在极端情况下可能暂停 WebGL 上下文。
- 如果上下文丢失时页面只剩黑屏，用户很难判断是作品加载慢、设备压力过大，还是浏览器正在恢复。
- 提示必须足够克制，不能破坏首屏作品化展示，也不能把主视觉重新变成调试界面。

### Solutions

- 在 `renderer.domElement` 上监听 `webglcontextlost` 和 `webglcontextrestored`，上下文暂停时阻止默认黑屏失败路径并发布状态。
- 新增 `webglStatus` React 状态，由 Three 渲染层通过 `setWebglStatus` 回传到控制面板。
- 控制面板仅在异常或刚恢复时显示低权重提示，正常状态保持隐藏，避免打扰主视觉。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把 WebGL 上下文恢复提示纳入工程验收。

### Learnings

- 高密 WebGL 作品不只要追求粒子规模和视觉密度，也要照顾浏览器上下文这种边界状态。
- 稳定性提示应该服务作品体验，而不是暴露过多技术细节；用户只需要知道系统正在恢复。
- 渲染层事件和 React UI 状态之间保持单向回传，可以在不扰乱 Three 主循环的情况下补足可观测性。

## 2026-05-31 第五期：自动演示镜头导演

### Challenges

- 自动演示已经能切换模式和城市记忆，但真实相机仍主要依赖巡航逻辑，开场段落的导演感还不够明确。
- 目标强调“蒜鸟从武汉城市网络中诞生”，序章需要让用户不操作也能依次看见星云、网络、城市骨架、蒜鸟主体和中心爆发。
- 新增镜头调度不能打断用户手动探索，也不能增加喧宾夺主的控制 UI。

### Solutions

- 为 `DEMO_PROLOGUE_SEQUENCE` 增加 `preset` 字段，让每一段序章绑定一个真实取景预设。
- 新增 `DEMO_CAMERA_SEQUENCE`，把自动演示的镜头脚本作为可验收的工程入口。
- 在 `publishDemoBeat()` 中同步发布 `cameraPresetSignal`，让模式、城市记忆和相机运动使用同一个时间戳推进。
- 保留手动取景预设的原逻辑，用户点击镜头仍会退出自动演示并接管探索。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，把自动演示镜头导演纳入验收闭环。

### Learnings

- 展演类 3D 作品的默认体验不只是自动旋转，相机需要像导演一样安排观看顺序。
- 模式、记忆点和相机三者同步后，复杂视觉层更容易被用户理解成一个连续故事。
- 不新增 UI 也能提升作品感；有时最好的控制面板改动，是把调度放进系统内部。

## 2026-05-31 第五期：目标覆盖审计脚本

### Challenges

- 当前项目能力已经很多，单靠人工记忆很难持续确认每次改动都没有削弱核心目标。
- 现有静态自检偏入口级检查，无法按“GraphPU 视觉底座、蒜鸟主体、武汉城市层、互动模式”等目标组展示覆盖情况。
- 审美目标不能被脚本完全替代，需要明确区分工程可验证项和仍需人工观看确认的部分。

### Solutions

- 新增 `scripts/audit-goal-coverage.mjs`，按七个目标组检查当前代码和文档是否覆盖关键能力。
- 新增 `npm run verify:goal`，把目标覆盖审计变成可独立执行的命令。
- 新增 `npm run verify:all`，串联静态自检、目标覆盖审计和生产构建，作为阶段交付前的完整验证入口。
- 更新 `ACCEPTANCE.md`，把目标覆盖审计纳入工程证据，同时保留审美类人工确认清单。
- 更新 `verify:static`，确保目标覆盖审计入口不会被后续改动漏掉。

### Learnings

- 当作品进入多层系统阶段，验收脚本本身也是作品工程的一部分。
- 目标覆盖审计不应该假装能判断“好不好看”，而应该稳定保护那些可以被代码证据证明的结构能力。
- 把长期目标拆成可检查的目标组，可以让后续继续打磨时更大胆，也更不容易偏离最初方向。

## 2026-05-31 第五期：审美巡检序列

### Challenges

- 验收文档中仍有蒜鸟第一眼识别度、GraphPU 参考强度、城市地标可读性和色彩比例等人工审美项。
- 这些目标不能完全交给静态脚本判断，但如果每次人工观看都靠临时乱点，很难形成稳定对比。
- 巡检入口必须克制，不能让控制面板变成说明书，也不能影响默认作品开场。

### Solutions

- 新增 `AESTHETIC_AUDIT_SEQUENCE`，用五段固定镜头检查蒜鸟主体、网络质感、武汉城市层、色彩比例和诞生爆发。
- 新增 `publishAuditBeat()`，让巡检段落同步切换模式、相机预设和城市记忆焦点。
- 控制面板新增“审美巡检”按钮，运行时使用现有短句读数，不新增大段说明。
- 审美巡检会退出演示模式并接管相机；用户手动切模式、取景或清除记忆时会自然退出巡检。
- 更新 `ACCEPTANCE.md`、`verify:static` 与 `verify:goal`，把审美巡检序列纳入工程证据链。

### Learnings

- 审美目标虽然不能被脚本替代，但可以被稳定的观看流程辅助判断。
- 作品调试工具也要有导演意识；固定巡检镜头能让团队更容易讨论同一个画面。
- 对这种高密 3D 视觉系统，人工审美确认和自动目标覆盖应该互补，而不是互相冒充。

## 2026-05-31 第五期：GraphPU 图计算波面

### Challenges

- 主网络已经有节点、边线、沿边流动和深度扫描，但“图谱正在运算”的整体波动还可以更明确。
- 新增层必须轻量，不能显著增加几何压力，也不能在蒜鸟态抢走主体轮廓。
- 计算波不能像普通装饰圆环，需要与网络态、城市态、爆发态和热点锁定的语义节奏产生关系。

### Solutions

- 新增 `GRAPH_COMPUTE_WAVE_COUNT` 和 `createGraphComputeWavefronts()`，用低透明环形波面包裹外围网络。
- 波面在网络态最强，城市态和爆发态适度出现，蒜鸟态自动压低，保持主体干净。
- 波面透明度绑定 `networkMix`、`cityMix`、`burstMix`、点击/模式波和热点锁定，让它像图计算波而不是固定装饰。
- 每个波面有不同半径、扁平比例、旋转方向和相位，形成从中心向外围传播的空间节奏。
- 更新 `ACCEPTANCE.md`、`verify:static` 与 `verify:goal`，把图计算波面纳入目标覆盖。

### Learnings

- GraphPU 感不只来自“更多线”，也来自让整张图谱看起来像正在被计算。
- 越接近最终作品，新增视觉层越要有模式权重；该出现时出现，该退后时退后。
- 少量几何加上节奏清晰的透明度控制，往往比继续堆粒子更能提升空间质感。

## 2026-05-31 第五期：审美显影与过曝压制

### Challenges

- 现有截图显示网络态和部分城市态已经有足够冲击力，但中心白光、Bloom 和高密网络线会吞掉蒜鸟轮廓。
- 如果继续加粒子或线条，GraphPU 感会更强，但蒜鸟和武汉城市层的第一眼识别度会下降。
- 浏览器工具本轮仍出现 webview attach 超时，只能依赖已有真实验证截图、服务状态和工程验证推进。

### Solutions

- 新增 `READABILITY_TUNING`，把核心光球、核心射线、网络曝光和蒜鸟前景锚点显影参数集中命名。
- 降低中心爆发球体和射线基础透明度，减少网络态/城市态大面积过曝。
- 进一步压低网络态与城市态的网络曝光、节点尺寸和核心粒子高光，让细线仍密集但不把主体糊成白块。
- 给 `suanbird-identity-anchors` 增加复杂模式下的前景显影权重，让蒜瓣、喙、眼点、蒜苗和翅膀在城市/网络层中更容易读出来。
- 更新 `ACCEPTANCE.md`，把审美显影参数作为当前阶段的可追溯验收点。

### Learnings

- 进入作品末段后，提升完成度不总是加层；有时更重要的是控制光量，让主题从已有系统里显出来。
- GraphPU 参考的震撼感和蒜鸟主题识别度之间需要动态平衡，参数必须按模式分权重。
- 审美调优也应该工程化命名，否则后续继续微调时很难判断哪些数字是在保护主体可读性。

## 2026-05-31 第五期：URL 取景与审美截图证据

### Challenges

- 内置浏览器连接不稳定，无法持续依赖手动点击控制面板来复现同一组审美镜头。
- 之前审美巡检虽然能自动切镜头，但缺少 URL 级入口，录屏、截图和回归对比仍不够稳定。
- Headless Chrome 在高密 WebGL 场景下容易超时或生成黑屏截图，截图流程不能直接当作压力测试。
- 第一轮真实截图暴露出中心白光仍然过大，会在蒜鸟态和诞生态吞掉部分主体轮廓。

### Solutions

- 新增 `readSceneUrlOptions()` 与 `URL_SCENE_PARAMS`，支持通过 `preset`、`mode`、`profile`、`density`、`demo`、`audit`、`immersive` 等 URL 参数复现固定镜头。
- 新增 `scripts/capture-audit-screenshots.mjs` 和 `npm run audit:screenshots`，用系统 Chrome 生成蒜鸟、网络、江汇城市、诞生四张固定审美截图。
- 截图脚本默认使用均衡档和较低截图密度，并加入基础黑屏体积检查，避免把空画布误当成验收证据。
- 根据截图反馈继续调低核心粒子的模式级叠加：蒜鸟态、城市态、网络态和爆发态分别控制有效粒子数量、点尺寸、透明度和核心增亮。
- 更新 `ACCEPTANCE.md` 与 `verify:static`，让 URL 取景和审美截图脚本进入可追溯验收链。

### Learnings

- 对视觉作品来说，稳定复现同一镜头和写代码本身一样重要；否则每次审美判断都容易漂移。
- 截图验证要和性能压测分开，审美证据优先保证可复现、非黑屏和构图一致。
- 真实截图比主观记忆更诚实，会直接暴露“中心爆发很酷但主体被吃掉”这类细节问题。

## 2026-05-31 第五期：目标距离评估

### Challenges

- 工程审计脚本已经显示目标覆盖全绿，但用户关心的是距离最终作品感还有多远，不能只用字符串覆盖来回答。
- 固定截图显示 GraphPU 密集网络、武汉江河与地标元素已经成立，但中心白光仍然会在部分镜头里吞掉蒜鸟轮廓。
- 当前项目已经进入后期调参阶段，后续差距更多来自审美强度、默认构图和现场性能稳定性，而不是基础功能缺失。

### Solutions

- 并行检查项目结构、目标文档、验收文档、复盘记录、主源码、样式和验证脚本。
- 执行 `npm run verify:all`，确认静态自检、目标覆盖审计和生产构建均通过。
- 人工查看 `audit-screenshots` 中蒜鸟、网络、江汇城市和诞生四个固定镜头，区分工程完成度与视觉完成度。
- 将当前距离判断归纳为：工程目标约 95% 完成，视觉作品目标约 85% 到 90% 完成，剩余主要集中在主体识别、光量控制、地标可读性和展演级性能复核。

### Learnings

- 高密粒子作品的最后 10% 不是继续堆功能，而是反复控制亮度、层次、镜头和主题可读性。
- 自动审计能保护结构能力，但“第一眼是不是蒜鸟”“城市记忆是否自然融入”仍需要人工观看。
- 当前系统已经适合进入展演化打磨阶段，下一步应优先做默认镜头和中心光量微调，而不是大改架构。

## 2026-05-31 第五期：蒜鸟显影与中心光量再平衡

### Challenges

- 蒜鸟态已经有完整结构，但中心白光、关系射线和高密网络仍会抢走第一眼注意力。
- 爆发态的冲击力足够强，但截图中中心白芯面积过大，容易把城市线稿和蒜鸟轮廓烧成一块。
- 城市态中的黄鹤楼和长江大桥需要更稳的可读性，同时不能变成旅游贴图或喧宾夺主的地标展示。

### Solutions

- 下调 `READABILITY_TUNING` 中核心光球、核心射线、网络曝光和粒子高光权重，减少中心白雾。
- 增加蒜鸟身份锚点的前景权重，新增蒜形身体左右外轮廓、眼点高光锚点，并把蒜苗主叶片纳入 `suanbird-identity-anchors`。
- 单独压低爆发态的核心粒子数量、粒子透明度、核心增亮、流轨和诞生涡旋权重，保留爆发但减少整块烧白。
- 提高黄鹤楼和长江大桥线稿在城市态中的权重，同时压低普通城市关系线，形成更清晰的主次。
- 完成 `npm run verify:all`、`npm run audit:screenshots` 和 in-app Browser 页面复核，确认构建、目标覆盖、截图和真实 Canvas 渲染均正常。

### Learnings

- 最后一阶段的视觉提升主要来自“减光”和“显影”，不是继续叠更多粒子。
- 蒜鸟识别需要稳定锚点：眼、喙、蒜苗、蒜瓣外轮廓必须在复杂模式中仍然站得住。
- 爆发态需要控制白芯面积；冲击力来自对比和回流结构，不只是亮度最大化。

## 2026-05-31 第五期：星云态 GraphPU 灰白网幕

### Challenges

- 用户反馈星云态仍然太亮，希望更接近参考图的黑底、灰白线网、金色节点和绿色微粒质感。
- 直接压低亮度会让画面变空，继续拉高原有网络密度又会带来性能压力，并且城市线稿会抢走抽象 GraphPU 感。
- 星云态需要先像纯粹图谱星云，武汉地标和江河叙事应更多留给城市态承接。

### Solutions

- 为星云态新增独立 `graphpu-nebula-veil`，由 5200 条灰白半透明线和 520 个金绿节点组成，只在星云态显现。
- 为核心粒子、网络节点和边线增加 `uNebulaStyle`，星云态使用灰白 / 金绿图谱配色，减少青绿过曝。
- 调整全景镜头，让星云态更贴近图谱网络而不是远景城市构图。
- 在星云态中大幅压低长江、汉水、城市线稿、记忆轨迹、诞生涡旋和城市编码雨，减少旅游线稿感。
- 完成 `npm run verify:all`，并生成 `audit-screenshots/nebula-tuned.png` 作为本轮视觉证据。

### Learnings

- 参考图的高级感来自黑场、灰白细线和节点层级，不是简单提高亮度。
- 不同模式需要不同视觉语言：星云态应抽象，城市态再显性表达武汉结构。
- 高密图谱可以通过独立轻量网幕补充，而不是让主网络承担所有审美目标。

## 2026-05-31 第五期：爆发态过曝修正

### Challenges

- 极限档爆发态下，核心粒子、Bloom、中心光球、关系射线、城市流轨和诞生涡旋叠加后形成大面积白块。
- 过曝区域会被控制面板遮挡，同时也吞掉了长江大桥、黄鹤楼和周围线网层次。
- 需要保留“诞生爆发”的能量感，但不能靠无节制亮度表达冲击。

### Solutions

- 降低 `burstParticleCountScale`，减少爆发态参与绘制的核心粒子数量。
- 爆发态下进一步压低 Bloom、核心粒子透明度、核心增亮、中心光球和关系射线强度。
- 收敛诞生涡旋、网络流动光点和城市流轨在爆发态的透明度与尺寸。
- 调整“诞生”取景预设，把相机稍微拉远并偏左，让中心亮核避开控制面板。
- 使用极限档、满密度、展开控制面板生成 `audit-screenshots/burst-tuned.png`，确认白块已明显收住。

### Learnings

- 爆发态的高级感来自“亮核周围仍有结构”，不是让中心完全烧白。
- 极限档必须单独验收，因为中档参数在高粒子量下会被非线性放大。
- 展示镜头也是控光的一部分，白核位置和控制面板关系需要一起考虑。
